const ClaimDisplay = ({ styles }) => {
  return (
    <div style={styles.section}>
      <h2 style={styles.title}>Claim</h2>
      <button style={styles.button}>Claim</button>
    </div>
  );
};
export default ClaimDisplay;
